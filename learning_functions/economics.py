# none return value if return is not written
def add_two_num(n1, n2):
    """
    this fun will add two no
    :param n1: This is the 1 no
    :param n2: this is the 2 no
    :return: will return n+1 | flot or int
    """
    total = n1 + n2
    return total

print(add_two_num(1, 2))

